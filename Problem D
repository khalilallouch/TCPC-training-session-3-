int main()
{
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int t;
    cin>>t;
    while(t--)
    {
        long long n,m,x;
        cin>>n>>m>>x;
        cout<<min(n/2,m)<<" "<<x*(n-min(n/2,m))*min(n/2,m)<<endl;
    }
 
    return 0;
}
